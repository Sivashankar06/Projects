<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.siva.foodapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--<application tools:replace="android:allowBackup"-->
        <!--android:allowBackup="false"-->
        <!--android:largeHeap="true"-->

    <application android:name=".FoodApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LauncherActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LocationActivity" android:theme="@style/LocateTheme" android:screenOrientation="portrait"/>

        <activity android:name=".HomeActivity" android:theme="@style/LocateTheme" android:screenOrientation="portrait"/>

        <activity android:name=".RestaurantsActivity" android:theme="@style/LocateTheme" android:screenOrientation="portrait"/>

        <activity android:name=".RestaurantDetailsActivity" android:theme="@style/LocateTheme" android:screenOrientation="portrait"/>

    </application>

</manifest>